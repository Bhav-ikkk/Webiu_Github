import { AnalyzerView } from '@/views';

export const metadata = {
  title: 'Analyzer | GitHub Repository Intelligence Analyzer',
};

export default function AnalyzerPage() {
  return <AnalyzerView />;
}
